{
  "name": "dao-graph",
  "version": "0.1.0",
  "scripts": {
    "build": "NETWORK=local NO_DEPLOY=true ./deploy.sh",
    "deploy:mainnet": "NETWORK=mainnet ./deploy.sh",
    "deploy:local": "NETWORK=local ./deploy.sh"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.21.1",
    "@graphprotocol/graph-ts": "^0.20.1",
    "mustache": "^4.2.0"
  }
}
